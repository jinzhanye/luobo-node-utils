const assert = require('assert');
const yjzNodeUtils = require('../index.js');

describe('yjzNodeUtils', () => {
  it('has a test', () => {
    assert(false, 'yjzNodeUtils should have a test');
  });
});
